'use strict';

var m = require('mithril');

module.exports = {
  controller: function () {
    var scope = {};
    return scope;
  },
  view: function (scope) {
    return m('h1', 'localBooking');
  }
};
